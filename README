Just some examples that can be used with cloudforms/manageiq and the embedded ansible.

all the cf_* are the variables that van be used in the catalog, then you only need to create a dialog with the name param_cf_*, the label and description of the inputfield van be anything.
